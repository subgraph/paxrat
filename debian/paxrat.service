[Unit]
Description=paxrat PaX management service for torbrowser-launcher
DefaultDependencies=no
After=systemd-remount-fs.service
Before=sysinit.target

[Service]
ExecStart=/sbin/paxrat -w -x -c /etc/paxrat/paxrat_tbl.conf

[Install]
WantedBy=multi-user.target

